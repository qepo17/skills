interface:
  display_name: "End-to-End Development"
  short_description: "Turn tickets into grounded specs and verified PRs"
  default_prompt: "Use $end-to-end-development to turn this ticket, spec, or request into a grounded implementation plan and deliver it through the durable workflow, asking only essential unresolved questions."
policy:
  allow_implicit_invocation: false
