[project]
name = "pi-end-to-end-development-orchestrator"
version = "0.1.0"
description = "Durable LangGraph control plane for the end-to-end-development Pi skill"
requires-python = ">=3.11"
dependencies = [
  "langgraph==1.2.11",
  "langgraph-checkpoint-sqlite==3.1.1",
]

[tool.uv]
package = false
